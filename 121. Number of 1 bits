class Solution {
public:
    int hammingWeight(int n) {
        bitset<128> binary_num(n);
        int count = 0;
        for (int i = 0; i < binary_num.size(); i++){
            if(binary_num[i] == 1)
                count++;
        }
    return count;
    }
};
